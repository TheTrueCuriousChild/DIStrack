/** Clinical Cartography: inventory is a searchable, FEFO-aware ledger where exception color is reserved for stock and expiry risk. */
import { useMemo, useState } from "react";
import { useLocation } from "wouter";
import { ArrowDownToLine, ArrowUpRight, Boxes, ChevronRight, ClipboardCheck, PackagePlus, Search, SlidersHorizontal, X } from "lucide-react";
import { toast } from "sonner";
import { DetailDrawer } from "@/components/DetailDrawer";
import { InlineError, PanelSkeleton } from "@/components/OperationalStates";
import { PageHeader, SectionHeading, StatusBadge } from "@/components/OperationalPrimitives";
import { useOperationData } from "@/hooks/useOperationData";
import { inventoryService } from "@/services/inventoryService";
import { drugs } from "@/services/mockData";

type Drug = (typeof drugs)[number];
const categories = ["All", "Analgesic", "Antibiotic", "Endocrinology", "Fluid Therapy"];

function StockBar({ percentage, health }: { percentage: number; health: string }) { return <div className="flex items-center gap-2"><div className="h-1.5 w-20 overflow-hidden rounded-full bg-[#e5eef4]"><div className={`h-full rounded-full ${health === "critical" ? "bg-[#c53030]" : health === "warning" ? "bg-[#b7791f]" : "bg-[#16803c]"}`} style={{ width: `${percentage}%` }} /></div><span className="data-number text-[10px] font-bold text-[#64748b]">{percentage}%</span></div>; }

export default function Inventory() {
  const [, navigate] = useLocation();
  const [category, setCategory] = useState("All");
  const [search, setSearch] = useState("");
  const [selected, setSelected] = useState<Drug | null>(null);
  const [adjustOpen, setAdjustOpen] = useState(false);
  const { data, loading, error, retry } = useOperationData(async () => (await inventoryService.listDrugs({ category, search })) as Drug[], [category, search]);
  const criticalCount = useMemo(() => (data ?? []).filter((drug) => drug.health === "critical").length, [data]);
  if (loading && !data) return <div className="page-enter"><PanelSkeleton lines={2} /><div className="mt-5"><PanelSkeleton lines={7} /></div></div>;
  if (error) return <InlineError message={error} retry={retry} />;
  return <div className="page-enter max-w-[1640px]">
    <PageHeader eyebrow="Operations / Inventory" title="Inventory ledger" subtitle="Batch-level stock, expiry posture, and immediate availability across the active facility." action={<button onClick={() => setAdjustOpen(true)} className="inline-flex h-10 items-center gap-2 rounded-md bg-[#1769aa] px-3.5 text-xs font-bold text-white shadow-sm hover:bg-[#0f5d9f]"><PackagePlus size={16} />Record stock count</button>} />
    <section className="mb-5 grid gap-3 md:grid-cols-3"><article className="surface-panel p-4"><p className="label-kicker">Tracked medicines</p><p className="data-number mt-2 text-2xl font-bold tracking-[-0.04em] text-[#0b4f88]">{data?.length ?? 0}</p><p className="mt-1 text-xs text-[#64748b]">Across 4 therapeutic categories</p></article><article className="surface-panel p-4"><p className="label-kicker">Critical availability</p><p className="data-number mt-2 text-2xl font-bold tracking-[-0.04em] text-[#c53030]">{criticalCount}</p><p className="mt-1 text-xs text-[#64748b]">Require same-day review</p></article><article className="surface-panel p-4"><p className="label-kicker">FEFO priorities</p><p className="data-number mt-2 text-2xl font-bold tracking-[-0.04em] text-[#a96810]">3</p><p className="mt-1 text-xs text-[#64748b]">Batches with expiry under 45 days</p></article></section>
    <section className="surface-panel route-ledger overflow-hidden"><div className="flex flex-col gap-4 border-b border-[#dce8f0] p-4 sm:p-5 lg:flex-row lg:items-center lg:justify-between"><div className="relative min-w-0 flex-1 lg:max-w-md"><Search size={17} className="absolute left-3 top-1/2 -translate-y-1/2 text-[#71879a]" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search medicine name or SKU" className="h-10 w-full rounded-md border border-[#cfe0ea] bg-white pl-9 pr-3 text-sm outline-none placeholder:text-[#90a2b0] focus:border-[#2196d2]" /></div><div className="flex gap-2 overflow-x-auto pb-1 lg:pb-0">{categories.map((item) => <button key={item} onClick={() => setCategory(item)} className={`h-9 whitespace-nowrap rounded-md border px-3 text-[11px] font-bold ${category === item ? "border-[#1769aa] bg-[#eaf5fc] text-[#0f5d9f]" : "border-[#dce8f0] bg-white text-[#587287] hover:bg-[#f6fafd]"}`}>{item}</button>)}<button onClick={() => toast.message("Advanced filters will be applied to the inventory service query.")} className="grid h-9 w-9 shrink-0 place-items-center rounded-md border border-[#dce8f0] text-[#547187] hover:bg-[#f6fafd]" aria-label="More inventory filters"><SlidersHorizontal size={16} /></button></div></div>
      <div className="overflow-x-auto scrollbar-thin"><table className="w-full min-w-[920px] text-left"><thead className="bg-[#f8fcfe]"><tr className="border-b border-[#dce8f0] text-[10px] font-bold uppercase tracking-[0.1em] text-[#6f899d]"><th className="px-5 py-3.5">Medicine</th><th className="px-4 py-3.5">Available stock</th><th className="px-4 py-3.5">Batch coverage</th><th className="px-4 py-3.5">Nearest expiry</th><th className="px-4 py-3.5">Storage</th><th className="px-4 py-3.5">FEFO priority</th><th className="px-5 py-3.5 text-right"> </th></tr></thead><tbody>{(data ?? []).map((drug) => <tr key={drug.id} className="border-b border-[#edf3f6] last:border-0 hover:bg-[#fbfdfe]"><td className="px-5 py-4"><button onClick={() => setSelected(drug)} className="text-left"><p className="text-[13px] font-bold text-[#102a43] hover:text-[#1769aa]">{drug.name}</p><p className="mt-1 text-[11px] font-medium text-[#71879a]">{drug.category} · {drug.sku}</p></button></td><td className="px-4 py-4"><p className="data-number text-[13px] font-bold text-[#102a43]">{drug.currentStock.toLocaleString()} <span className="font-medium text-[#71879a]">{drug.unit}</span></p><p className={`mt-1 text-[10px] font-semibold ${drug.trend < 0 ? "text-[#a96810]" : "text-[#16803c]"}`}>{drug.trend > 0 ? "+" : ""}{drug.trend}% vs. 7-day avg.</p></td><td className="px-4 py-4"><p className="data-number text-[12px] font-bold text-[#102a43]">{drug.batchCount} active batches</p><div className="mt-2"><StockBar percentage={drug.stockPercent} health={drug.health} /></div></td><td className="px-4 py-4"><p className={`data-number text-[12px] font-bold ${drug.expiryDays < 15 ? "text-[#c53030]" : drug.expiryDays < 50 ? "text-[#a96810]" : "text-[#102a43]"}`}>{drug.nearestExpiry}</p><p className="mt-1 text-[10px] text-[#71879a]">{drug.expiryDays} days remaining</p></td><td className="px-4 py-4 text-[12px] font-medium text-[#426076]">{drug.storage}</td><td className="px-4 py-4"><StatusBadge status={drug.fefo.includes("Immediate") ? "critical" : drug.fefo.includes("High") ? "warning" : "healthy"} label={drug.fefo} /></td><td className="px-5 py-4 text-right"><button onClick={() => setSelected(drug)} className="inline-flex h-8 items-center gap-1 rounded px-2 text-[11px] font-bold text-[#1769aa] hover:bg-[#eaf5fc]">Detail <ChevronRight size={14} /></button></td></tr>)}</tbody></table></div></section>
    <DetailDrawer open={Boolean(selected)} onClose={() => setSelected(null)} title={selected?.name ?? "Medicine detail"} subtitle={selected ? `${selected.category} · ${selected.sku}` : undefined}>{selected && <div className="space-y-6"><section className="grid grid-cols-2 gap-3 sm:grid-cols-4">{[{ label: "Available stock", value: `${selected.currentStock.toLocaleString()}`, detail: selected.unit }, { label: "Reorder level", value: selected.reorderLevel.toLocaleString(), detail: "units" }, { label: "Nearest expiry", value: `${selected.expiryDays} days`, detail: selected.nearestExpiry }, { label: "Storage band", value: selected.storage, detail: "required" }].map((item) => <div key={item.label} className="rounded-md border border-[#dce8f0] bg-white p-3"><p className="label-kicker">{item.label}</p><p className="data-number mt-2 text-[15px] font-bold text-[#102a43]">{item.value}</p><p className="mt-1 text-[10px] text-[#71879a]">{item.detail}</p></div>)}</section><section><SectionHeading label="Batch ledger" title="Available batches" detail="Issued in first-expiry-first-out order." /><div className="mt-3 overflow-hidden rounded-md border border-[#dce8f0] bg-white">{selected.batches.map((batch, index) => <div key={batch.batchNo} className={`p-4 ${index ? "border-t border-[#edf3f6]" : ""}`}><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-mono text-[11px] font-bold text-[#0b4f88]">{batch.batchNo}</p><p className="mt-1 text-xs font-medium text-[#426076]">{batch.manufacturer}</p></div><StatusBadge status={batch.status === "Near expiry" ? "critical" : batch.status === "High priority" ? "warning" : "healthy"} label={batch.status} /></div><div className="mt-3 grid grid-cols-3 gap-2 text-[11px]"><div><p className="label-kicker">Manufactured</p><p className="mt-1 text-[#426076]">{batch.manufacturingDate}</p></div><div><p className="label-kicker">Expires</p><p className="mt-1 text-[#426076]">{batch.expiryDate}</p></div><div><p className="label-kicker">Quantity</p><p className="data-number mt-1 font-bold text-[#102a43]">{batch.quantity.toLocaleString()}</p></div></div></div>)}</div></section><section><SectionHeading label="Recent ledger" title="Stock movements" /><div className="mt-3 space-y-3">{selected.movements.map((movement) => <div key={`${movement.date}-${movement.reference}`} className="flex items-start gap-3 rounded-md border border-[#e3edf3] bg-white p-3"><span className={`mt-0.5 grid h-7 w-7 place-items-center rounded ${movement.type.includes("Consumption") || movement.type.includes("out") ? "bg-amber-50 text-[#a96810]" : "bg-emerald-50 text-[#16803c]"}`}>{movement.type.includes("Consumption") || movement.type.includes("out") ? <ArrowUpRight size={15} /> : <ArrowDownToLine size={15} />}</span><div className="min-w-0 flex-1"><p className="text-xs font-bold text-[#102a43]">{movement.type} <span className="data-number text-[#1769aa]">{movement.quantity}</span></p><p className="mt-1 text-[11px] text-[#64748b]">{movement.reference}</p></div><time className="data-number text-[10px] text-[#8195a5]">{movement.date}</time></div>)}</div></section><button onClick={() => { setSelected(null); navigate("/alerts"); }} className="inline-flex h-10 items-center gap-2 rounded-md border border-[#b9d4e5] bg-white px-3.5 text-xs font-bold text-[#1769aa] hover:bg-[#eaf5fc]"><ClipboardCheck size={15} />Review related alerts</button></div>}</DetailDrawer>
    {adjustOpen && <div className="fixed inset-0 z-50 grid place-items-center bg-[#071827]/40 p-4 backdrop-blur-[1px]"><form onSubmit={(event) => { event.preventDefault(); setAdjustOpen(false); toast.success("Stock count recorded in the local operational ledger.", { description: "Connect VITE_INVENTORY_API_URL to persist this adjustment." }); }} className="w-full max-w-md rounded-xl border border-[#c8dce8] bg-white p-5 shadow-2xl"><div className="flex items-start justify-between"><div><p className="label-kicker">Inventory action</p><h2 className="mt-2 text-lg font-bold text-[#0b1f33]">Record stock count</h2></div><button type="button" onClick={() => setAdjustOpen(false)} className="rounded p-1.5 text-[#64748b] hover:bg-[#f2f8fc]"><X size={18} /></button></div><p className="mt-3 text-sm leading-5 text-[#64748b]">This action creates an inventory-count adjustment when a live inventory service is configured.</p><label className="mt-5 block text-xs font-bold text-[#426076]">Medicine<select required className="mt-1.5 h-10 w-full rounded-md border border-[#cfe0ea] bg-white px-3 text-sm outline-none focus:border-[#2196d2]"><option value="">Select medicine</option>{drugs.map((drug) => <option key={drug.id} value={drug.id}>{drug.name}</option>)}</select></label><label className="mt-4 block text-xs font-bold text-[#426076]">Counted quantity<input required min="0" type="number" placeholder="0" className="mt-1.5 h-10 w-full rounded-md border border-[#cfe0ea] px-3 text-sm outline-none focus:border-[#2196d2]" /></label><div className="mt-6 flex justify-end gap-2"><button type="button" onClick={() => setAdjustOpen(false)} className="h-10 rounded-md px-3 text-xs font-bold text-[#64748b] hover:bg-[#f2f8fc]">Cancel</button><button className="h-10 rounded-md bg-[#1769aa] px-3.5 text-xs font-bold text-white hover:bg-[#0f5d9f]">Save count</button></div></form></div>}
  </div>;
}
